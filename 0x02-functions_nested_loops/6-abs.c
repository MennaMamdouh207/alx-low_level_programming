#include "main.h"
/**
 * _abc - Computes the bslute valeu
 *@c: the number will be computed
 *Return: Abslute of valeu
 *
 */
int _abc(int c)
{
if (c < 0)
{
int abc_val;
abc_val = c * -1;
return (abc_val);
}
return (0);
}
